name: 03 Schedule
on:
  schedule:
            #┌───────────── minuto (0-59)
            #│ ┌───────────── hora (0-23)
            #│ │ ┌───────────── día del mes (1-31)
            #│ │ │ ┌───────────── mes (1-12)
            #│ │ │ │ ┌───────────── día de la semana (0-7) (dom=0 o 7)
            #│ │ │ │ │

    - cron: '6 7 * * 1' # Mala mia por el chiste del 67 
  workflow_dispatch:
jobs:
  schedule:
    runs-on: ubuntu-24.04
    steps:
      - name: Saludar
        run: echo "Hola desde GitHub Actions!"
        